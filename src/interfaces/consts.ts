export interface QuestionTypeConstProps{
    TEXT: string,
    TEXTAREA: string,
    RADIO: string,
    CHECKBOX: string,
    DATE: string,
    SELECT: string,
    SCALE: string,
    DESCRIPTION: string,
    IMAGE: string
}

export const QuestionTypeConst: QuestionTypeConstProps = {
    TEXT: 'text',
    TEXTAREA: 'textarea',
    RADIO: 'radio',
    CHECKBOX: 'checkbox',
    DATE: 'date',
    SELECT: 'select',
    SCALE: 'scale',
    DESCRIPTION: 'description',
    IMAGE: 'image'
}